#include<iostream>
using namespace std;
void reverse(string str)
{
	int len=str.length();
	int n=len;
	while(n--)
	   cout<<str[n];
}
int main(void)
{
	string s="geeks for geeks";
	reverse(s);
	return(0);
}
